# Yahoo Fantasy Sports API Credentials
# Get these from https://developer.yahoo.com/apps/create/
YAHOO_CONSUMER_KEY=your_consumer_key_here
YAHOO_CONSUMER_SECRET=your_consumer_secret_here

# Yahoo Fantasy League Configuration
YAHOO_LEAGUE_ID=94443
YAHOO_TEAM_ID=9
YAHOO_GAME_CODE=nba

# Email Notification (for --watch mode)
# Uses Gmail SMTP with an App Password (NOT your Google account password).
# To create an App Password:
#   1. Enable 2-Factor Authentication on your Google account
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Create an app password for "Mail"
#   4. Paste the 16-character password below (no spaces)
NOTIFY_EMAIL_TO=
NOTIFY_SMTP_PASSWORD=
# Optional overrides (defaults work for Gmail):
# NOTIFY_EMAIL_FROM=        # Defaults to NOTIFY_EMAIL_TO
# NOTIFY_SMTP_HOST=smtp.gmail.com
# NOTIFY_SMTP_PORT=587
